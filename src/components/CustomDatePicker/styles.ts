import { styled } from '@mui/material';
import { Box } from '@mui/system';
import { DatePicker } from '@mui/x-date-pickers';

export const DatePickerContainer = styled(Box)({
  width: '100%'
});

export const CustomDatePickerComponenet = styled(DatePicker)({
  width: '100%'
});
